export interface InitSettings {
  bRule: number[],
  sRule: number[],
  seedQty: number,
  seedArea: number[],
  maxFPS: number,
}

export interface RenderSettings {
  scale: number,
  pos: number[],
  blur: number
}